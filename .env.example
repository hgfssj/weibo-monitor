# 国家队 ETF / 股指期货 / 宏观 等数据采集均为公开接口，无需任何密钥即可运行。
# 以下为「可选」配置，仅需启用大模型研判时设置；不设置则自动降级为启发式，不影响数据采集。

# 启用大模型研判（大V观点总结 / 黑话过滤）：设置后代码内置 urllib 直连 DashScope
# DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx
# 模型覆盖（可选，默认 qwen-plus）
# DASHSCOPE_MODEL=qwen-plus
